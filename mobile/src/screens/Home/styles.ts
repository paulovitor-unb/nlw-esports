import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: { flex: 1, alignItems: "center" },
    logo: { marginTop: 10, marginBottom: 10, width: 214, height: 120 },
    contentList: { paddingHorizontal: 32 },
});
